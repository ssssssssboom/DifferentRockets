-- v2026.07.27
-- Structural strut. Behavior: none.
-- v2026.07.27 (round 23): the per-part joint override (24 Hz / 1.25) was
-- REMOVED — joint properties are now unified for every connection at
-- 28 Hz / 1.0 / 1.0 (see joints.lua DEFAULT_* and physics.lua `joints`).
function onLoad(part)
end

function onStage(part)
end

function onUpdate(part, dt)
end
