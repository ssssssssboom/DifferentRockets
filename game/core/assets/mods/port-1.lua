-- v2026.07.27
-- Docking connector (port side). Behavior: none.
-- v2026.07.27 (round 23): the soft-connection override (8 Hz / 0.9,
-- matching dock-1) was REMOVED — joint properties are now unified for
-- every connection at 28 Hz / 1.0 / 1.0 (see joints.lua DEFAULT_* and
-- physics.lua `joints`).
function onLoad(part)
end

function onStage(part)
end

function onUpdate(part, dt)
end
